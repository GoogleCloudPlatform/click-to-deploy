matrix:
  include:
  - language: python
    python: 2.7
    install: pip install jinja2 pyyaml
    env: TARGET=python-test
  - language: ruby
    install: gem install foodcritic
    env: TARGET=vm-lint
  - language: ruby
    rvm:
    - 2.5
    before_install: gem install knife-solo
    env: TARGET=vm-generate-triggers

script: make "${TARGET}"
