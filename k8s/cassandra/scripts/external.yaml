apiVersion: v1
kind: Service
metadata:
  name: cassandra-1-cassandra-external-svc
  labels:
    app.kubernetes.io/name: "cassandra-1"
    app.kubernetes.io/component: cassandra-server
spec:
  ports:
  - name: cql
    port: 9042
  selector:
    app.kubernetes.io/name: "cassandra-1"
    app.kubernetes.io/component: cassandra-server
  type: LoadBalancer
