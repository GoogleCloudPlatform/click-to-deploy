properties:
  testerImage:
    type: string
    default: $REGISTRY/tester:$TAG
    x-google-property:
      type: IMAGE
  oAuth:
    clientID: null
    clientSecret: null
    clientIDEncoded: null
    clientSecretEncoded: null

  updateStrategy:
    type: RollingUpdate

  service:
    type: NodePort
    port: 8080

  certificate:
    hostname: addykube2.1bot2.info

  controller:
    serviceAccount: null

  setupAttributes:
    initialEmail: hassaan@google.com
