{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "addy.serviceAccountName" . }}
  labels:
{{ include "addy.labels" . | nindent 4 }}
{{- end -}}
