apiVersion: v1
kind: Service
metadata:
  name: {{ include "addy.fullname" . }}
  labels:
    {{- include "addy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "addy.selectorLabels" . | nindent 4 }}
