apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: basic-ingress
  annotations:
    networking.gke.io/managed-certificates: example-certificate1
spec:
  backend:
    serviceName: web
    servicePort: 80
