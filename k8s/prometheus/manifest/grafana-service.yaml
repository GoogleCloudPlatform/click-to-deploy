apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    kubernetes.io/name: "Grafana"
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    k8s-app: grafana
