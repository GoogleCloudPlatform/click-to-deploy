apiVersion: v1
kind: Service
metadata:
  name: $APP_INSTANCE_NAME-grafana
  labels:
    k8s-app: grafana
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    k8s-app: grafana
