apiVersion: v1
kind: Pod
metadata:
  name: es-client
spec:
  containers:
  - name: es-client
    image: debian:latest
    command:
    - /bin/bash
    - -ec
    - "apt-get update; apt-get install -y curl; while :; do echo '.'; sleep 10 ; done"
  restartPolicy: Never