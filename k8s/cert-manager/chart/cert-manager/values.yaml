# Default values for cert-manager.
image:
  repository: null
  tag: null

# Replica count for components
controller:
  replicaCount: 1

webhook:
  replicaCount: 1

cainjector:
  replicaCount: 1

metrics:
  image: null
  exporter:
    enabled: false