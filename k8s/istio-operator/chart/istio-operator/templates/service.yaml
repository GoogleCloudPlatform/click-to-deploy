apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: '{{ .Release.Name }}'
    name: istio-operator
  name: istio-operator
  namespace: '{{ .Values.operatorNamespace }}'
spec:
  ports:
  - name: http-metrics
    port: 8383
    targetPort: 8383
  selector:
    name: istio-operator
