properties:
  testerImage:
    type: string
    default: $REGISTRY/tester:$TAG
    x-google-property:
      type: IMAGE
  admin.bindAllHosts:
    type: boolean
    title: Bind all hosts
    default: true
